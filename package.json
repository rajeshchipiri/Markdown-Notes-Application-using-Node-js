{
  "name": "markdown-notes-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
